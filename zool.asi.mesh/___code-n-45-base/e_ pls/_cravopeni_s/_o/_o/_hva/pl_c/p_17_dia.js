export function sen_ese(b, c) {
    if (/\d/.test(b)) {
        return (parseInt(b) - c + 10) % 10;
    }
    return b;

    
}
 
function mayb(foryou) {
    const but_for_sum_us = 'theres only today';
}

